#!/usr/bin/env bash
set -euo pipefail

app="spikes/macos-shell/.build/SatinApplication"
result_file="spikes/macos-shell/.build/native-terminal-nvim-quit.result"

rm -f "$result_file"

SATIN_NATIVE_SMOKE_SCENARIO=terminal-nvim-quit \
  SATIN_NATIVE_SMOKE_RESULT="$result_file" \
  "$app" >"/tmp/satin-terminal-nvim-quit.log" 2>&1

result=$(cat "$result_file")
echo "$result"
[[ "$result" == ok* ]]
