{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -x \"${SATIN_CLI:-}\" ] && \"$SATIN_CLI\" status event claude >/dev/null 2>&1 || true",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -x \"${SATIN_CLI:-}\" ] && \"$SATIN_CLI\" status event claude >/dev/null 2>&1 || true",
            "timeout": 5
          }
        ]
      }
    ],
    "PermissionRequest": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -x \"${SATIN_CLI:-}\" ] && \"$SATIN_CLI\" status event claude >/dev/null 2>&1 || true",
            "timeout": 5
          }
        ]
      }
    ],
    "Elicitation": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -x \"${SATIN_CLI:-}\" ] && \"$SATIN_CLI\" status event claude >/dev/null 2>&1 || true",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -x \"${SATIN_CLI:-}\" ] && \"$SATIN_CLI\" status event claude >/dev/null 2>&1 || true",
            "timeout": 5
          }
        ]
      }
    ],
    "StopFailure": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -x \"${SATIN_CLI:-}\" ] && \"$SATIN_CLI\" status event claude >/dev/null 2>&1 || true",
            "timeout": 5
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -x \"${SATIN_CLI:-}\" ] && \"$SATIN_CLI\" status event claude >/dev/null 2>&1 || true",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
