#!/usr/bin/env bash
set -euo pipefail

app="spikes/macos-shell/.build/SatinApplication"
result_file="spikes/macos-shell/.build/native-terminal-nvim-handoff.result"

rm -f "$result_file"

SATIN_NATIVE_SMOKE_SCENARIO=terminal-nvim-handoff \
  SATIN_NATIVE_SMOKE_RESULT="$result_file" \
  "$app" >"/tmp/satin-terminal-nvim-handoff.log" 2>&1

result=$(cat "$result_file")
echo "$result"
[[ "$result" == ok* ]]
